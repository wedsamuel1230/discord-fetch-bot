services:
  discord-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      # Persist runtime state (cache/history/artifacts) across container restarts
      - ./data:/app/data
